{
  "name": "cc-board",
  "version": "0.4.0",
  "description": "A local desktop bridge for native Claude Code",
  "author": "cc-board contributors",
  "license": "MIT",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "start": "npm run build && electron .",
    "test": "node --test tests/*.test.cjs",
    "test:desktop": "npm run build && node tests/desktop-smoke.cjs",
    "test:desktop-native": "npm run build && node tests/desktop-native.cjs",
    "test:native": "node tests/native-bridge.cjs",
    "dist": "npm run dist:win",
    "dist:win": "npm run build && electron-builder --win nsis --x64 --publish never",
    "dist:mac": "npm run build && electron-builder --mac dmg zip --publish never",
    "pack": "npm run build && electron-builder --dir --win --x64",
    "pack:mac": "npm run build && electron-builder --dir --mac"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "electron": "44.3.0",
    "electron-builder": "26.15.3",
    "lucide-react": "^0.468.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "vite": "^7.3.0"
  },
  "build": {
    "appId": "io.ccboard.desktop",
    "productName": "cc-board",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "electron/**/*",
      "package.json",
      "LICENSE"
    ],
    "asarUnpack": [
      "electron/permission-server.cjs"
    ],
    "win": {
      "target": "nsis",
      "executableName": "cc-board",
      "icon": "build/icon.ico"
    },
    "mac": {
      "target": [
        "dmg",
        "zip"
      ],
      "icon": "build/icon.png",
      "category": "public.app-category.developer-tools",
      "minimumSystemVersion": "13.0.0",
      "darkModeSupport": true,
      "identity": null,
      "artifactName": "cc-board-${version}-mac-${arch}.${ext}"
    },
    "dmg": {
      "title": "cc-board ${version}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "artifactName": "cc-board-Setup-${version}.${ext}"
    }
  }
}
